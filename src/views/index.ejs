<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AAAAA</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>
<body>
    <div class="topnav">
        <a class="active" href="#home">Home</a>
        <a href="#news">News</a>
        <a href="#contact">Contact</a>
        <a href="#about">About</a>
      </div>
      <%= dataUser.length %>
      <div style="padding-left:16px">
        <h2>List User</h2>
      </div>
      <a href="/create-user">Them moi user</a>
      <div class="w3-container">
        <h2>Table</h2>
      
        <table class="w3-table-all w3-hoverable">
          <thead>
            <tr class="w3-light-grey">
              <th>ID</th>
              <th>First Name</th>
              <th>Last Name</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% for(let i=0 ; i < dataUser.length ; i++ ) { %>
              <tr>
                <td>
                  <%= dataUser[i].id %>
                </td>
                <td>
                  <%= dataUser[i].fname %>
                </td>
                <td>
                  <%= dataUser[i].lname %>
                </td>
                <td>
                  <a href="/detail/user/<%=dataUser[i].id %>" ><button>Detail</button></a>
                  
                  <a href="/update-user/<%= dataUser[i].id %>"><button>Update</button></a>
                  
                  <form action="/delete-user" method="post">
                    <input type="text" hidden value="<%=dataUser[i].id %>" name="userId">
                    <button>Delete</button>
                  </form>
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <!-- <a href="/create-user"><button>Create User</button></a> -->
</body>
</html>